services:
  # Frontend React App (Standalone - No Backend)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: car-dealership-frontend
    restart: unless-stopped
    ports:
      - "80:80"

